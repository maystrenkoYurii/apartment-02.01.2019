import React, { Component } from 'react';

import Wrapper from '../../../../containers/Pages/Wrapper';

class Organizations extends Component {
  render() {
    return <Wrapper> {'Organizations'} </Wrapper>;
  }
}

export default Organizations;
